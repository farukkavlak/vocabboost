{
  "name": "extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build --config vite.background.config.ts && vite build --config vite.content.config.ts && vite build --config vite.settings.config.ts && vite build --config vite.offscreen.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^26.5.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.3.0"
  }
}
